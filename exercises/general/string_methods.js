/*
const input = 'fantastica';

function idChars(input, target)
{
    let count = new Array;
    for (let i =0; i <= input.length; i++)
    {
        if (input[i] === target)
        {
            count.push(input[i]);
        }  
    }
    return count.length;
}
*/

var two = 2;
console.log(two);
var two = 'two';
console.log(two);
