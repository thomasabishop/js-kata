import DisplayReading from './DisplayReading.js';

const query = new DisplayReading();

//console.log(query.readingRaw);
//query.hexagramFull;
query.hexagramPresent;
//console.log(query.checkChangingLines());
/*
 * Need a numerical basis for looking up the hexagrams
 * Need to write a script that will generate each of the 64 hexagrams
 * Find all possible permutations of array [6,7,8,9]
 */
